"use client";
import { useState } from "react";
export function EvidenceForm() { const [content,setContent]=useState(""); const [message,setMessage]=useState(""); const [saving,setSaving]=useState(false); async function submit(){setSaving(true);setMessage(""); const r=await fetch("/api/evidence",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({content})}); const d=await r.json(); setSaving(false); setMessage(r.ok?`评分 ${d.assessment.score}/100：${d.assessment.feedback}\n下一任务：${d.nextTask.title}——${d.nextTask.instruction}`:d.error); if(r.ok)setContent("");} return <><textarea aria-label="学习证据" value={content} onChange={e=>setContent(e.target.value)} placeholder="粘贴笔记、代码片段、链接，或直接回答老师的问题…" style={{display:"block",width:"100%",minHeight:160,padding:12}} /><button type="button" disabled={saving||!content.trim()} onClick={submit} style={{marginTop:12}}>{saving?"保存中…":"提交给老师"}</button>{message&&<p style={{whiteSpace:"pre-line"}}>{message}</p>}</>; }
